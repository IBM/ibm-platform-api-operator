apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-api-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: platform-api-operator
  template:
    metadata:
      labels:
        name: platform-api-operator
    spec:
      serviceAccountName: platform-api-operator
      containers:
        - name: platform-api-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "platform-api-operator"
